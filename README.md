# VERALYA Portfolio Website

A responsive, static luxury-brand case study built with semantic HTML, CSS, and vanilla JavaScript.

## Run locally

Open `index.html` directly, or serve this folder with any static web server.

The product imagery is used unchanged and is stored in `assets/` as optimized WebP copies.
